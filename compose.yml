services:
  app:
    build: .
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - path: ./.env
  db:
    image: postgres:18
    volumes:
      - postgres:/var/lib/postgresql
    environment:
      - "POSTGRES_PASSWORD=password"

volumes:
  postgres:
